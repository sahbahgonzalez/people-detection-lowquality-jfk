# Task 3 – Counting People

This task counts people per frame using YOLO detections + heatmap smoothing.

## Files
- `counting_MAE.ipynb`
- `eval_ground_truth_counts.csv`
- `example_frames/` (small sample only)

## Output
- MAE (Mean Absolute Error)
- Per-frame comparison with ground truth
